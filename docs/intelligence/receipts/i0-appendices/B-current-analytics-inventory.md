# CURRENT ANALYTICS INVENTORY (backend, as of main@ba208963)

## 1. Navigation analytics (only dedicated event-analytics pipeline in the codebase)
- **Ingest**: `POST /api/analytics/navigation` — PUBLIC (no auth), per-IP rate limit 120/min, 32KB body cap, ≤50 events/batch, schemaVersion=1 gated. `backend/routes/navigationAnalyticsRoutes.js:43`
  - Writes `navigation_analytics_events` (migration `database/migrations/20260623130000_navigation_analytics_events.sql`). Allowlist-projected columns only: schema_version, event_type (9 enum types: surface_opened, item_impression, item_selected, destination_rendered, destination_blocked, role_switched, drawer_opened, tab_selected, error), feature_id, node_id, surface, source/destination_route_pattern, platform, role_category, lifecycle_or_reason_code, build_version, occurred_at. `navigationAnalyticsService.js:37-66,185-198`
  - role_category derived SERVER-side from trusted session via `resolveRequestContext` (client role ignored) `navigationAnalyticsService.js:286-294`; routes collapsed to registered manifest patterns or `'unregistered'`; no PII/tenant/device/URL persisted. In-process best-effort dedupe (2000-key window) `:211-228`. Storage failure swallowed (always 202).
  - Emitters: `web/src/lib/navigationAnalytics.ts` and `mobile/utils/navigationAnalytics.ts`.
- **Read**: `GET /api/admin/analytics/navigation` — `authorizeRole(['admin'])`. `navigationAnalyticsRoutes.js:98-112`
  - Metrics (`getNavigationAggregates`, `navigationAnalyticsService.js:310-374`): totals {events, impressions, selections, destinationRenders, blocked, errors}, selectionThroughRate (selections/impressions, 4dp), platformSplit, roleCategorySplit, eventTypeSplit, topSurfaces (top 10), zeroSelectionItems (impressed-but-never-selected feature/node ids).
  - Windowing: `from`/`to` query (default last 7 days), optional `feature` filter; single bounded read (limit 50,000 rows) then in-memory aggregation → **derived, not authoritative at scale** (silent truncation past 50k rows/window).
  - Tenant scoping: none — platform-wide by design (no tenant column exists). Admin-only.
  - Consumer: `web/src/components/admin/NavigationAnalyticsPanel.tsx:109` rendered inside `web/src/pages/dashboard/admin/FeatureGovernanceConsole.tsx`.

## 2. Marketplace admin analytics
- `GET /api/admin/marketplace/analytics` — `authorizeRole(['admin','government'])` + service-layer `assertModerator`. `backend/routes/marketplaceAdminRoutes.js:68`, `backend/services/marketplace/marketplaceAnalyticsService.js:21-63`
  - Sources: full scan of `vehicles` (vin,status,trust_score,owner_id,tenant_id) with fixture exclusion; full scan of `marketplace_inquiries` (best-effort — zeroes + note if table missing).
  - Metrics: listings {total, public, pending_review, suppressed, rejected, archived, high_risk (0<trust_score<50)}; inquiries {total, by_type (12 inquiry types), by_status (7 statuses), referral_attributed (has referral_code), high_risk (risk_status≠clear), diaspora_demand (5 diaspora inquiry types)}; `generated_at`.
  - Windowing: none — all-time snapshot, computed live per request (derived-in-JS, unbounded row fetch).
  - Tenant scoping: none — platform-wide; `government` role sees platform-wide counts.
  - Consumer: `web/src/pages/dashboard/admin/MarketplaceModeration.tsx:208` via `fetchMarketplaceAnalytics` (`useCarUpApi.ts:622`).

## 3. Communications analytics (user-facing)
- `GET /api/communications/analytics` — `authorizeRole([])` (any authenticated user). `backend/routes/communicationBaseRoutes.js:210-216`
  - Service: `CommunicationAnalyticsService.getUserAnalytics` (`backend/services/communication/communicationAnalyticsService.js:38-155`). Scope = threads the CALLING USER participates in (`message_participants.user_id = req.userContext.id`), then optional tenant filter `!row.tenant_id || row.tenant_id === tenantId` (tenantless threads included). tenantId from `req.userContext.tenantId` = `x-tenant-id` header **validated against `tenant_users` membership** in `authMiddleware.js:150-162` → server-validated, not P0.
  - Sources: message_participants, message_threads, conversation_events, notification_queue, message_derivations, communication_campaign_deliveries. Row cap 1000 (query `limit` up to 2000) per table — **bounded/derived, not authoritative**; `bounded:true, row_cap` returned.
  - Metrics: conversations {total, active, converted, conversion_rate_pct, unique_participants, by_workflow, by_funnel_stage}; marketplace {conversations, converted, inquiry_to_next_step_pct}; response_time {measured, average_minutes, median_minutes, p95_minutes} from `first_response_at - created_at`; events {total, by_type}; delivery {total, successful, failed, suppressed, success_rate_pct, by_channel, by_status}; attribution {by_source, by_referral_code, by_campaign_code} (from conversation_events columns); ai {derivations, human_reviewed, by_type}; campaigns {touches, suppressed, converted, conversion_rate_pct, by_status}.
  - Windowing: none (all rows within cap).
  - Consumer: `web/src/hooks/useCommunicationProductApi.ts:103` → `web/src/pages/dashboard/owner/Communications.tsx` (analytics state, lines 95/144-171).

## 4. Communications admin metrics / SLA / ops
All below in `backend/routes/adminCommunicationRoutes.js`, gate `ADMIN_ROLES = ['admin','platform_admin','super_admin','support','finance','trust_manager','compliance_manager','marketplace_manager']` (`:13`). Tenant scoping via `resolveListScope` (`:65-73`): platform base-role admins → unscoped (all tenants); others → `tenant_id = userContext.tenantId` (membership-validated header); no tenant → **403 fail-closed**. Server-derived, not client-supplied.
- `GET /api/admin/communications/metrics` (`:1337-1354`): open_threads, unassigned_threads, overdue_threads (`sla_due_at < now` and not resolved/closed), dead_letter_count, duplicate_webhook_count. Sources: message_threads, notification_queue(status=dead_letter), webhook_logs — full list fetch, counted in JS (comment concedes counts-RPC preferable at scale). No time window. Consumer: `fetchAdminCommunicationMetrics` (`useCarUpApi.ts:2282`) → `web/src/pages/dashboard/admin/Communications.tsx:149,303,366` (header stats).
- `GET /api/admin/communications/worker/health` (`:957-1009`): queue {queued, processing, retry_scheduled, dead_letter, depth, oldest_queued_seconds, sla_threshold_seconds (env COMMUNICATION_SLA_SECONDS default 60), sla_breaching}, adapter health, scheduler health via RPC `get_communication_scheduler_health` (graceful fallback). Sources: notification_queue (limits 500/100/100/100). Tenant-scoped queue, global scheduler facts. Consumer: `fetchCommunicationWorkerHealth` → admin Communications.tsx:282.
- `GET /api/admin/communications/providers` (`:1035-1064`): per-channel provider telemetry (latest inbound/outbound/error, queue/retry/dead-letter counts, credential presence) via `buildProviderTelemetry` over notification_queue (limit 1000), webhook_logs (500), message_delivery_attempts (1000, scoped via notification ids because table has no tenant_id); stale_locks count. Consumer: `fetchCommunicationProviders`.
- SLA machinery: `communicationSla.js` pure state machine (healthy/due_soon/breached/paused/not_applicable; targets first_response/next_response/resolution/sla_due_at; DUE_SOON=15min). **`computeSlaState` is NOT served by any backend route today** — only `pausePatch`/`resumePatch` are imported (`communicationThreadService.js:2`); the frontend mirrors the badge logic. Per-thread SLA aggregates (breach rates, response-time percentiles per tenant/agent) do NOT exist as an endpoint; only `overdue_threads` count above and per-user response_time in §3.
- `GET /api/admin/communications/audit` (`:1013`), `/sla/policies` (`:1024`): tenant-scoped LISTS (not aggregates).

## 5. Referral analytics
- **No referral KPI/aggregate endpoint exists.** What exists (`backend/routes/referralRoutes.js`, mounted at `/api/referrals` via `promotionsRoutes.js:68`):
  - `GET /api/referrals/admin/events` (`:511-514`) — `authorizeRole(['admin','platform_admin','super_admin'])`; raw event TIMELINE (list, limit ≤200 default) from `referral_events` via `getAdminTimeline` (`referralEngineService.js:742-749`). Filters tenant_id/campaign_id/code_id/event_type are **client-supplied query params** (`req.query.tenant_id || userContext.tenantId || undefined` → no filter = all tenants). Gate is platform-admin-only, so cross-tenant read is by-design, not P0 — but note the tenant filter is convenience, not boundary.
  - `GET /api/referrals/wallets/:userId` (`:491`) — self-or-admin; wallet row + last 100 transactions (`referralEngineService.js:650-655`). Balances maintained by `recomputeWalletBalances` (sum by status bucket, `:657-671`) → stored aggregate on `referral_wallets`.
  - `POST /api/referrals/marketing/analytics/suggestions` (`:374`) — operator-gated; AI/benchmark suggestion generator, not a measurement read.
  - Consumers: `web/src/pages/dashboard/owner/ReferralWallet.tsx`, `web/src/pages/dashboard/admin/ReferralCodes.tsx`.
- **P0 (integrity)**: `POST /api/referrals/events` (`referralRoutes.js:155-158`) is **UNAUTHENTICATED and un-rate-limited**: any caller can insert arbitrary rows into `referral_events` with client-chosen `event_type`, `code_id`, `campaign_id`, `tenant_id`, `metadata`, `occurred_at`, and actor_type via `x-actor-type` header (`recordReferralEvent`, `referralEngineService.js:379-399` — no validation of code/campaign existence, tenant falls back to client value). The entire referral/attribution "analytics" stream (incl. `referral_attributed` counts in §2 and the admin timeline) is therefore client-forgeable.

## 6. Platform admin stats
- `GET /api/admin/stats` — `authorizeRole(['admin'])`. `backend/routes/adminRoutes.js:33-54`. Metrics: totalUsers, totalVehicles, totalEscrows (counts `escrow_trust_sessions` — canonical post-#164), totalClaims (`insurance_claims`) via Supabase `count:'exact', head:true` (authoritative-live counts); plus **hardcoded fiction**: `systemHealth:'Optimal'`, `aiConfidence:'98.5%'` (`:51-52`) — not measurements. No windowing, no tenant scope (platform-wide). Consumer: `fetchAdminTelemetry` (`useCarUpApi.ts:2347`) → `web/src/pages/dashboard/admin/AdminDashboard.tsx:32`.
- `GET /api/admin/health` (`:70`) — full `server_health` table dump.
- `GET /health` (public, `server.js:235-260`): in-memory `metricsHub.getSnapshot()` (`backend/services/metrics.js` — process-local outbox/OCR/webhook/trust-engine counters, reset on restart, NOT durable), plus live `domain_events` pending count.

## 7. Diaspora aggregates (tenant-scoped operator reads)
- `GET /api/diaspora/trade-graph/summary` + `/projection/status` (`backend/routes/diasporaTradeGraphRoutes.js:250-261`) — entitlement-gated; node/edge counts by type via SQL GROUP BY on `trade_graph_nodes`/`trade_graph_edges`, projection lag/health (HEALTHY/DEGRADED/STALLED thresholds 300s/1800s), dead-letter count. Tenant id **server-derived, enforced in SQL**; service throws without it (`diasporaTradeGraphHealthService.js:30-33,41-60`). Counts-only shape (no ids) — authoritative-live.
- `GET /api/diaspora/workbook/operator-dashboard` (`diasporaWorkbookRoutes.js:121`): totals {totalBatches, readyForReview, draftsImported, failedDraftImports, cancelled, held} computed in JS over ≤1000 batches scoped by userContext (`diasporaWorkbookOperatorConsoleService.js:60-78`).
- `GET /api/diaspora/subscriptions/billing-health` (`diasporaSubscriptionRoutes.js:277-296`): failedWebhooks/supersededWebhooks counts+events, reconciliation freshness, checkout funnel — `checkoutFunnelSummary` (`diasporaBillingCheckoutSessionService.js:195-217`): counts {open, completed, abandoned, expired, cancelled}, abandonmentRate over decided sessions. Tenant from validated membership (`requireTenantId`, routes`:88-94`).
- `GET /api/diaspora/safetrade/outbox` (`diasporaSafeTradeRoutes.js:456`): backlog summary (oldestPendingAgeSeconds).

## 8. Partner/stakeholder "summary" endpoints (projections, not KPI analytics)
- Partner API (`backend/routes/partnerApiRoutes.js:86-120`): `/trust-summary`, `/fraud-summary` (fraud_status, conflicts, open_cases, reason_codes), `/source-coverage`, `/dealers/:id/summary` — scope-gated derived trust projections per VIN/dealer, not aggregates.
- `GET /api/dealers/:id/summary` (`dealerRoutes.js:153`): buyer-safe {status, compliance_review_date, evidence_completeness_band, **unresolved_serious_complaints: 0 hardcoded** (`dealerComplianceService.js:374`)}.
- **NO dealer, insurance, bank, or government KPI/analytics dashboard backend exists.** insurerRoutes/lenderRoutes expose per-VIN eligibility/decision reads and admin decision LISTS only; governmentActivationRoutes exposes source health/error LISTS. No counts/aggregate endpoints for these roles.

# LISTING-VIEW MEASUREMENT REALITY
- Confirmed: `marketplace_listing_viewed` exists ONLY as a referral-attribution event. Emission path: `GET /api/marketplace/listings/:id` (`marketplaceRoutes.js:104-120`) → `referralContextFromReq` reads **client-supplied** `?ref/referral_code/campaign_code/source` (`:37-46`) → **only if `referralCode || campaignCode` is present** → `marketplaceReferralBridge.emitMarketplaceReferralEvent` (best-effort, `.catch(()=>{})`) → `ReferralEngineService.recordReferralEvent` → insert into `referral_events` (`marketplaceReferralBridgeService.js:77-139`, table `referralEngineRepository.js:6`).
- Web caller: `VehicleDetail.tsx:1347-1349` (`captureReferralFromUrl()`/`getStoredAttribution()` → passes ref/campaign/source to `fetchMarketplaceListingDetail`, `useCarUpApi.ts:547-551`).
- Consequences (verified absences): an unattributed view (no ref/campaign) is recorded NOWHERE — no view counter, no listing_views/page_views table, no `view_count` on vehicles (only `marketing_unsubscribe_tokens.view_count` exists, unrelated: `20260817200000_marketing_unsubscribe_tokens.sql:37`). No endpoint aggregates `marketplace_listing_viewed` (it only appears in the raw admin timeline §5 and `getMarketplaceAnalytics` counts inquiries, not views). Listing "views" as a seller/admin metric DOES NOT EXIST.

# SAVED/UNSAVE + SHARE INSTRUMENTATION REALITY
- **Save/unsave**: `POST|DELETE /api/marketplace/listings/:id/save` (`marketplaceRoutes.js:142-148`, auth required) → `marketplaceSavedService.js`: insert/delete on `saved_vehicles` (user_id, vin, created_at; UNIQUE(user_id,vin) migration 010) ONLY. Verified: no history/audit row, no event emission, no counter — an unsave leaves zero trace; save-count-per-listing is not computable retroactively and no endpoint computes it even currently.
- **Share**: web marketplace share buttons (`Marketplace.tsx:43-44`, `VehicleDetail.tsx:1415-1418,1902`) use `navigator.share`/clipboard **client-side only — no network call, no durable record**. Backend `POST /api/communications/share` exists (`communicationBaseRoutes.js:249-285`; queues an in_app `listing_shared` notification_queue row with listing/ref metadata) but its web hook `createCommunicationShare` (`useCarUpApi.ts:2184`) has **zero page consumers** — dead wiring. Referral `SHARE_KIT_PREPARED` events (`referralChannelGatewayService.js:192-201`) fire only in referral share-kit/channel-gateway flows (ReferralWallet/ReferralCodes pages), not on marketplace listing shares. **No durable marketplace share event exists.**

# CROSS-CUTTING FINDINGS
- P0 integrity: unauthenticated `POST /api/referrals/events` forges the attribution stream (§5).
- Client-supplied attribution: ref/campaign codes on listing view + inquiry are attacker-controllable query params (expected for attribution, but all downstream "referral_attributed" analytics inherit that trust).
- Every analytics read except `/api/admin/stats` counts and trade-graph SQL GROUP BYs is **derived-in-JS over bounded row fetches** (caps: 50k nav, 1k comms, 1k/500 ops lists, unbounded vehicles/inquiries scan in §2) — silent undercount at scale.
- Cosmetic/fabricated metrics presented as data: `aiConfidence '98.5%'`, `systemHealth 'Optimal'` (`adminRoutes.js:51-52`); `unresolved_serious_complaints: 0` (`dealerComplianceService.js:374`).
- No warehouse/rollup/materialized-view layer exists anywhere; no scheduled aggregation jobs for analytics; `backend/services/metrics.js` is process-memory only.