# I0 — External-Integration Reality Matrix

Audited at `feat/carup-intelligence-1-0` @ 47d68f0d (content == main@ba208963). DB evidence queried read-only against staging + production Supabase (2026-08-27). Prod schema is MISSING the entire Full Activation provider platform (`provider_registry`, `government_source_config`, `insurer_profiles`, `lender_profiles`, `escrow_provider_config` do not exist in prod). `provider_registry` = **0 rows in staging** → no provider (even sandbox) is registered anywhere; every provider-platform call fails closed to `not_contracted` (`backend/services/sourceVerification/governmentActivation.js:12-22`). Real transports for gov/insurance/finance/escrow are an explicit stub: "'credential_pending' -> 'unavailable' until wired" (same file, header §1); the only invoker is the deterministic simulator (`backend/services/providerPlatform/providerFramework.js:45-47`, `simulators.js`).

## Matrix

| Integration | Classification | Evidence | What changes the class | Intelligence-plan relevance (I0–I19) |
|---|---|---|---|---|
| CVR gov vehicle registry | BUILT-BUT-INACTIVE (sandbox-simulated, fail-closed) | `governmentActivation.js` (SOURCE_KEYS incl `cvr`), `providerPlatform/{providerRegistry,providerFramework,simulators}.js`; migration `20260703130000_government_source_activation.sql` (`government_source_config`, staging 0 rows, prod table ABSENT); `cvr_ownership_records` 0 staging / 0 prod; `provider_request_attempts` 0 | Signed contract + `provider_registry` row + real transport implementation + credentials + prod migration | I15 Government/Regulatory Intelligence; verified facts feed I6/I7 trust surfaces |
| CID police clearance | BUILT-BUT-INACTIVE | Same governed path (`cid` in SOURCE_KEYS; CID access append-only logging §6 of `governmentActivation.js`); `cid_clearance_records` 0/0 | Same as CVR | I15; stolen-vehicle/risk signals for I6, I16 |
| ZIMRA customs/duty (API) | BUILT-BUT-INACTIVE | `zimra` in SOURCE_KEYS; `zimra_declarations` 0 staging / 0 prod | Same as CVR | I15; import-duty intelligence for I13 |
| ZIMRA customs docs (OCR path) | EVIDENCE-BASED-ONLY (document upload + OCR extraction, human review) | `ocr_customs_declarations`: **11 rows in prod**, 0 staging; written by `document-intelligence/documentIntelligenceService.js` via `ai/GeminiClient.js`; extraction compare/human-review in `evidence/extractionService.js` ("no extraction auto-approves") | A real ZIMRA API contract (would merge into row above) | I15, I13; existing rows are the only real customs data today |
| ZINARA licensing | BUILT-BUT-INACTIVE | `zinara` in SOURCE_KEYS; `zinara_licensing_records` 0/0 | Same as CVR | I15 |
| VID roadworthiness | BUILT-BUT-INACTIVE | `vid` in SOURCE_KEYS; `vid_inspections` 0/0 | Same as CVR | I15; condition signals for I6/I9 |
| OCR pipeline (provider) | PARTIAL (real client code, key-dependent; historical prod use) | Real Gemini endpoint `generativelanguage.googleapis.com/...gemini-2.5-flash` (`backend/services/ai/GeminiClient.js:70,128`); `OCR_PRIMARY_PROVIDER=gemini`, `OCR_FALLBACK_PROVIDER=groq`, `ALLOW_OCR_MOCK` (`backend/env.example`); mock gated to `NODE_ENV==='test'` only (`documentIntelligenceService.isOcrMockAllowed`); prod `ocr_customs_declarations` 11 rows prove past real runs; key presence not verifiable from repo | Verified live `GEMINI_API_KEY`/`GROQ_API_KEY` in staging/prod env → LIVE | I18 Gutu AI; I15 evidence extraction; I2 event capture of OCR outcomes |
| Insurance providers | BUILT-BUT-INACTIVE | `20260703140000_insurance_provider.sql`; `insurance/{insuranceService,insurerWorkflow}.js` (portal decision path); `insurer_profiles` 0 staging / prod ABSENT; `insurance_provider_decisions` 0/absent; eligibility sandbox provider only (`eligibility/eligibilityService.js:14-21` builds ONLY `buildSandboxEligibilityProvider`) | Real insurer onboarded (profile row + portal use) or real provider webhook creds; prod migration | I10 Insurance Intelligence |
| Banks/lenders (finance) | BUILT-BUT-INACTIVE | `20260703150000_finance_provider.sql`; `finance/{financeService,lenderWorkflow}.js`; `lender_profiles` 0 / prod ABSENT; `finance_provider_decisions` 0; `eligibility_requests` 6 staging / 2 prod (sandbox/UAT runs); `eligibility_webhook_events` 0/0; inbound HMAC+replay webhook security exists (`eligibility/webhookSecurity.js`) | Real lender onboarded; first authentic webhook event | I11 Finance Intelligence |
| Payments/escrow — SafeTrade | BUILT-BUT-INACTIVE (sandbox, doubly fail-closed) | `20260621130000_diaspora_phase9_safetrade.sql:58,74` — `live_payment boolean DEFAULT false` + `CHECK (live_payment = false)`; RPC rejects non-`sandbox`/`fake` provider (line 390); `diaspora_safetrade_transactions` 0 staging / 0 prod | EB-4 activation migration + approved provider + contracts/KYC (per migration header) | I13 Diaspora/Trade; transaction events for I2/I4 |
| Payments/escrow — provider platform | BUILT-BUT-INACTIVE | `escrow/escrowProviderService.js` header: "No real funds move until approved provider + signed contracts + KYC/AML + real credentials — none of which are invented here"; `escrow_provider_config` 0 staging / prod ABSENT; `20260703160000_escrow_provider.sql` | Registered live escrow provider + ACTIVE config + prod migration | I16 command center money-flow view |
| Payments — external PSP (Stripe/Paynow/EcoCash) | NOT-PRESENT (names as constants only, live list frozen empty) | `backend/constants/diaspora/diasporaBillingConstants.js:39` `APPROVED_LIVE_PROVIDERS = Object.freeze([])` → sandbox selected in EVERY env; legacy gateway webhook retired with 410 (`payment/paymentRouter.js:24-30`); legacy SafePay authority disabled shim (`safepay/escrowService.js` `LEGACY_SAFEPAY_DISABLED`); residual legacy `/api/safepay/webhook` in `server.js:1595` (default secret, internal escrow status only — no external PSP calls) | PSP contract + entry in APPROVED_LIVE_PROVIDERS + webhook creds | I13, I11 |
| Email — Resend (transactional/conversational) | LIVE/CONNECTED (staging); prod NOT activated | Real adapter `api.resend.com` (`communication/adapters/providerAdapters.js:320,363`); inbound `resendWebhookService.js`/`resendInboundContentService.js`; staging `notification_queue`: resend delivered 10 / failed 1; prod `notification_queue` = 46 rows ALL `queued`, provider NULL — **zero sends ever in prod** | Prod: env creds + worker enabled + deploy (Communications 2.0 not prod-deployed) | I2 activity ledger, I14, I17 next-best-action delivery channel |
| Email — Brevo (marketing only) | LIVE/CONNECTED (staging, low volume) | Adapter `api.brevo.com` (`providerAdapters.js:391,474`); staging nq: brevo delivered 2 / dead_letter 2 / sent 1; marketing-only routing (`EmailTransportRouter`, `MARKETING_EMAIL_CLASSIFICATIONS`) | Prod activation | I14 Referral & Marketing Intelligence |
| Email — SendGrid | BUILT-BUT-INACTIVE (superseded) | Adapter `providerAdapters.js:275,284`; env template still defaults `EMAIL_PROVIDER=sendgrid` but Email 1.0 canonical transports are Resend/Brevo (env.example "§0A"); 0 sendgrid rows in either env nq | Deliberate fallback activation | Fallback only |
| Email — Cloudflare (worker + adapter) | PARTIAL | `CloudflareEmailAdapter` (`providerAdapters.js:571`); worker source `cloudflare/carup-communications-edge/src/index.js` (inbound email ≤25MB, attachment blocklist) with only `wrangler.toml.example` (deployment not evidenced in repo); 0 nq rows | Deployed worker + env secrets + observed traffic | Inbound email capture for I2 |
| WhatsApp — Meta Cloud API | LIVE/CONNECTED (staging); prod inactive | `MetaWhatsAppAdapter` → `graph.facebook.com/v20.0` (`providerAdapters.js:711,723`); staging nq `meta_whatsapp_cloud_api`: delivered 5 / sent 3 / dead_letter 13 / suppressed 1 (PR #148 gate C certified real sends); prod nq has zero sends | Prod deploy + Meta creds in prod | I2, I17 outbound; I16 comms telemetry |
| Telegram bot | LIVE/CONNECTED (staging) | `TelegramBotAdapter` → `api.telegram.org` (`providerAdapters.js:784,794`); `assertRealTelegramAdapter` guard (line 836); staging nq: sent 6 / delivered 1 | Prod activation | I2 |
| FB Messenger / Instagram messaging | BUILT-BUT-INACTIVE | Adapters `providerAdapters.js:737,760` (graph.facebook.com); zero nq rows either env | Page creds + observed sends | I14 |
| SMS — Twilio | BUILT-BUT-INACTIVE | `TwilioSmsAdapter` (`providerAdapters.js:654-694`); TWILIO_* env vars empty in templates; zero sms nq rows | Twilio account + creds | I17 fallback channel |
| Push — Expo | PARTIAL (adapter real; failing/unconfigured) | `ExpoPushAdapter` → `exp.host` (`providerAdapters.js:808-829`); staging nq: push dead_letter 16, provider NULL; `PUSH_PROVIDER=expo`, `EXPO_ACCESS_TOKEN` empty | Valid token + registered device tokens → delivered rows | I17 |
| Shipping/logistics + container | NOT-PRESENT (external); internal record-keeping only | Tables `diaspora_container_shipments`/`diaspora_shipments` (`database/migrations/013_diaspora_trade_schema.sql:217,276`) 0 rows staging AND prod; populated only via workbook import services (`diaspora/diasporaWorkbook*.js`); no shipping-line/tracking API calls anywhere (no http in `diasporaShipmentService.js`/`diasporaContainerService.js`) | Any carrier/tracking API client | I13 Diaspora/Trade Intelligence |
| Google Drive (diaspora workbooks) | BUILT-BUT-INACTIVE (code-complete, fails closed) | Full OAuth+REST client: `diaspora/drive/googleOAuthClient.js:26-28`, `googleDriveProvider.js:36-37` (`googleapis.com/drive/v3`); env gated `DIASPORA_DRIVE_ENABLED=false`, GOOGLE_* commented out, vault REQUIRED in prod (`env.example`); `diaspora_drive_connections` 0 staging / 0 prod | Google OAuth creds + flag on + vault backend + first connection row | I13; workbook ingestion feeding I2/I4 |
| Blockchain (external chain) | NOT-PRESENT — internal signature ledger misnamed "blockchain" | `blockchain/blockchainService.js`: node `crypto` secp256k1 keypairs stored in Supabase `public_keys` (incl. `private_key_pem` in DB — flag for security review), events in `blockchain_events`; NO web3/ethers/RPC/chain refs anywhere. Table ACTIVE: 23 rows staging / **716 rows prod** (internal ledger is live; external chain is not) | An actual chain/anchor service integration | Provenance signals for I5/I15; do not describe as blockchain in stakeholder docs |
| Partner API (referral/marketing partners) | BUILT-BUT-INACTIVE (gateway real; zero real partners) | `partner/partnerAuthService.js` (hashed keys, scopes, append-only audit), `20260626130000_partner_api.sql`; staging `partner_clients` 3 rows all `uat-partner-*` (UAT artifacts, active); prod 2 rows `uat-prod-*` both REVOKED; `partner_api_requests` 8 staging | First real partner issued a key | I14; partner telemetry into I2 |
| Listing ingestion — JP auction source | BUILT-BUT-INACTIVE (fixture-backed by design) | `ingestion/adapters/sandboxJpAuctionAdapter.js:1-12`: "NO live auction API and NO credentials", `mode: 'fixture'`; promotion path documented in header | Real auction API impl + credentials + contract verification | I3 marketplace instrumentation; supply intelligence I8/I13 |
| Automation webhooks (n8n) | BUILT-BUT-INACTIVE | `ENABLE_AUTOMATION_WEBHOOKS=false`, `AUTOMATION_PROVIDER=n8n` (`env.example`); `eventBus/automationWebhookService.js` `dispatchAutomationWebhook` | Flag on + webhook URL | I17 action dispatch |
| AI — Gemini / Groq (comms + fraud + OCR) | PARTIAL (real clients; key-dependent) | `ai/GeminiClient.js:70`, `communication/communicationGeminiProvider.js:36`, `communicationGroqProvider.js` (`api.groq.com`); `aiServiceBus.js` logs `gemini-pro`/`gemini-vision` inferences | Verified keys in env + inference log rows | I18 Gutu AI; I16 |
| AI — OpenRouter / Moonshot | PLANNED/ASPIRATIONAL (env flags only) | Only presence checks: `server.js:266-267`, `metrics.js:89-90`; `.env.example` keys; NO client code | An actual client implementation | I18 |
| Gov "registry_verifications" / "compliance_reports" (legacy Domain-3) | Demo/seed data, NOT real integrations | **Confirmed seeds**: `database/migrations/008_domain3.sql:28-38` INSERTs — placeholder VINs `1HGCM82633A004XXX`/`JTDKB20U43023XXXX`, user `u5`, "Interpol database cleared" notes, fake report URLs. Staging counts (2/2) exactly match the seeds; prod `registry_verifications` = 3 (2 seeds + 1 other). No service writes them (only `routes/complianceRoutes.js` reads) | Nothing — legacy demo tables; exclude from any "connected" claim | I0 must mark these as demo in the inventory |

## Cross-cutting facts for the synthesizer
- **Prod schema gap**: entire provider platform (gov/insurance/finance/escrow config + registry tables) exists in staging only; prod has none of it. Any Intelligence phase consuming provider outcomes has literally no prod substrate yet.
- **Runtime provider truth (staging nq)**: resend 11, meta_whatsapp 22, telegram 7, brevo 5; everything else 0. **Prod nq: 46 queued, 0 ever sent** — Production Communications inactive, matching memory (Communications 2.0 never prod-deployed).
- `source_verification_results`: 3 rows staging / 1 prod (sandbox/UAT-scale, not a live feed). `provider_request_attempts`: 0.
- Honesty architecture is structural: sandbox results can never be relabelled live (`governmentActivation.js` MODE_MAP; simulators label `mode`), so no doc/table row found here overstates itself — the overstatement risk is purely in prose docs, not code.
- Observation worth escalating outside I0: `blockchainService.js` persists `private_key_pem` in the `public_keys` table.