import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
process.env.NODE_ENV='test';process.env.SUPABASE_URL=process.env.SUPABASE_URL||'http://localhost:54321';process.env.SUPABASE_SERVICE_ROLE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||'test-key';
const tx=await import('../services/transaction/marketplaceTransactionAuthority.js');
const LISTING={vin:'PHASE6VIN0000001',owner_id:'seller-owner',current_seller_id:null,publication_status:'published',price:12500,currency:'USD',currency_source:'seller',updated_at:'2026-08-19T00:00:00.000Z'};
const INQUIRY={id:'11111111-1111-4111-8111-111111111111',listing_id:LISTING.vin,buyer_id:'buyer-1',seller_id:'seller-owner',inquiry_type:'vehicle_purchase_interest',status:'new',risk_status:'clear'};
test('Phase 6: seller resolution is server-side and deterministic',()=>{assert.equal(tx.resolveMarketplaceSellerId(LISTING),'seller-owner');assert.equal(tx.resolveMarketplaceSellerId({...LISTING,current_seller_id:'current-seller'}),'current-seller');assert.equal(tx.resolveMarketplaceSellerId({}),null);});
test('Phase 6: transaction amount/currency require recorded server terms plus provenance',()=>{assert.deepEqual(tx.resolveMarketplaceListingTerms(LISTING),{amount:12500,currency:'USD',currencySource:'seller'});assert.throws(()=>tx.resolveMarketplaceListingTerms({...LISTING,price:0}),/server-authoritative transaction amount/);assert.throws(()=>tx.resolveMarketplaceListingTerms({...LISTING,currency_source:null}),/provenance-backed transaction currency/);});
test('Phase 6: only current clear purchase inquiry binds buyer seller listing',()=>{assert.equal(tx.isCurrentPurchaseInquiry(INQUIRY,{vin:LISTING.vin,buyerId:'buyer-1',sellerId:'seller-owner'}),true);assert.equal(tx.isCurrentPurchaseInquiry({...INQUIRY,risk_status:'watch'},{vin:LISTING.vin,buyerId:'buyer-1',sellerId:'seller-owner'}),false);assert.equal(tx.isCurrentPurchaseInquiry({...INQUIRY,seller_id:'old-seller'},{vin:LISTING.vin,buyerId:'buyer-1',sellerId:'seller-owner'}),false);});
test('Phase 6: snapshot changes when money/counterparty fact changes',()=>{const terms=tx.resolveMarketplaceListingTerms(LISTING);const a=tx.buildMarketplaceListingSnapshot(LISTING,'seller-owner',terms);assert.notEqual(a,tx.buildMarketplaceListingSnapshot({...LISTING,price:13000},'seller-owner',{...terms,amount:13000}));assert.notEqual(a,tx.buildMarketplaceListingSnapshot(LISTING,'other-seller',terms));});
test('Phase 6: canonical transaction idempotency is server-derived and truth-sensitive',()=>{const base={vin:LISTING.vin,buyerId:'buyer-1',sellerId:'seller-owner',inquiryId:INQUIRY.id,listingSnapshotHash:'snap-1',gateContext:{identity_status:'complete'}};const a=tx.buildCanonicalTransactionKey(base);assert.equal(a,tx.buildCanonicalTransactionKey(base));assert.notEqual(a,tx.buildCanonicalTransactionKey({...base,inquiryId:'22222222-2222-4222-8222-222222222222'}));assert.notEqual(a,tx.buildCanonicalTransactionKey({...base,gateContext:{identity_status:'not_evaluated'}}));});
test('Phase 6: Trust dimensions become explicit escrow gates',()=>{assert.deepEqual(tx.buildMarketplaceEscrowGateContext({dimensions:{identity:{status:'complete'},publication_eligibility:{status:'publishable'},fraud_risk:{status:'clear'},dealer_compliance:{status:'compliant'},evidence_completeness:{status:'complete'}}}),{identity_status:'complete',publication_status:'publishable',fraud_block:false,seller_suspended:false,participant_authorized:true,required_documents_present:true,listing_snapshot_changed:false});});
test('Phase 6: public transaction projection omits participant/provider internals',()=>{const p=tx.toPublicMarketplaceEscrowSession({id:'tx-1',vin:LISTING.vin,status:'eligible',buyer_id:'private-buyer',seller_id:'private-seller',listing_amount:12500,listing_currency:'USD',escrow_id:null,payment_provider:'private-provider-id',gate_reasons:[],created_at:'now',updated_at:'now'});assert.equal(p.transaction_intent_id,'tx-1');assert.equal(p.payment_state,'not_started');for(const key of ['buyer_id','seller_id','payment_provider'])assert.equal(key in p,false);});
test('Phase 6: escrow request route accepts no client transaction truth or idempotency authority',()=>{const source=fs.readFileSync(new URL('../routes/escrowTrustRoutes.js',import.meta.url),'utf8');const a=source.indexOf("router.post('/api/vehicles/:vin/escrow'");const b=source.indexOf("router.get('/api/vehicles/:vin/escrow'",a);const block=source.slice(a,b);assert.ok(a>=0&&b>a);for(const forbidden of ['buyer_id','seller_id','participant_authorized','required_documents_present','listing_snapshot_hash','idempotency-key'])assert.equal(block.includes(forbidden),false,`request route must not accept ${forbidden}`);assert.match(block,/requestMarketplaceEscrow/);});
test('Phase 6: transition route does not pass client gate_context',()=>{const source=fs.readFileSync(new URL('../routes/escrowTrustRoutes.js',import.meta.url),'utf8');const a=source.indexOf("router.patch('/api/escrow/:id/transition'");const b=source.indexOf("router.post('/api/escrow/webhook'",a);const block=source.slice(a,b);assert.ok(a>=0&&b>a);assert.equal(block.includes('req.body?.gate_context'),false);assert.match(block,/serverGateContextFor/);});
